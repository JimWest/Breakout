#pragma once

// Helper Class for Math stuff
class MathUtility
{
public:
	// Returns he absolute value of a number (makes it positive)
	//
	// @param value The number that should be absolute.
	// @return The absolute number of the value.
	static int abs(int value);

	// Returns he absolute value of a number (makes it positive)
	//
	// @param value The number that should be absolute.
	// @return The absolute number of the value.
	static double abs(double value);
};

